# Dependencies / build output
node_modules/
.expo/
dist/
web-build/
coverage/
*.log

# Environment — .env.example is committed, real values never are
.env
.env.*
!.env.example

# Model artefacts — hundreds of MB, never committed (CLAUDE.md §8)
*.tflite
*.keras
*.h5
server/models/

# Training image datasets never belong in the app repo. The copy that landed
# here is also the PRE-AUGMENTED balanced set (1,406 files, aug_*.jpg) that
# docs/Tomato_Updated_Code_Review.md Issue 3 identifies as the wrong dataset
# to train on. Keeping it out prevents anyone picking it up by accident.
Balance DataSet Without Healthy Class/
Balance DataSet Without Healthy Class.zip
*/tomato__JAS_MIT/
*/tomato__LM/
*/tomato__MIT/
*/tomato__N_K/

# Expo / Metro / native (from the SDK 54 template)
expo-env.d.ts
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.metro-health-check*
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo
/ios
/android

# OS noise
.DS_Store
Thumbs.db
*.pem

# Python — server venv only; never global installs (see GO_AHEAD_pip_and_phases.md)
__pycache__/
*.pyc
.venv/
server/.venv/
.pytest_cache/

# Assembled Hugging Face Space folder (generated by npm run space:prepare).
# The sources are deploy/huggingface/ and server/app/ — this is a build output.
.hf-space/
