# Dependencies / build output
node_modules/
.expo/
dist/
web-build/
coverage/
*.log

# Environment — .env.example is committed, real values never are
.env
.env.*
!.env.example

# Model artefacts — hundreds of MB, never committed (CLAUDE.md §8)
*.tflite
*.keras
*.h5
server/models/

# Expo / Metro / native (from the SDK 54 template)
expo-env.d.ts
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.metro-health-check*
npm-debug.*
yarn-debug.*
yarn-error.*
*.tsbuildinfo
/ios
/android

# OS noise
.DS_Store
Thumbs.db
*.pem

# Python — server venv only; never global installs (see GO_AHEAD_pip_and_phases.md)
__pycache__/
*.pyc
.venv/
server/.venv/
.pytest_cache/
